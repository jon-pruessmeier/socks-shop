version: "3"

services:

  frontend:
    build: ./shop-frontend
    restart: always
    ports:
      - "4321:80"
    depends_on:
      - api
    links:
      - "api:backend"


  api:
    build: ./shop-backend
    restart: always
    ports:
      - "4322:4200"

